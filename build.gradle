buildscript {
    repositories {
        jcenter()
    }
}

subprojects {
    apply plugin: 'java'

    group = project.modGroup
    version = project.modVersion

    sourceCompatibility = '1.8'
    targetCompatibility = '1.8'

    repositories {
        mavenCentral()
    }
}
